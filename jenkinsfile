pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code...'
                checkout scm
            }
        }

        stage('Test Backend') {
            steps {
                echo 'Testing Flask backend...'
            }
        }

        stage('Build Frontend') {
            steps {
                echo 'Building Next.js frontend...'
            }
        }
    }
}
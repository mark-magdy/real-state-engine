import { ROIAnalysis } from '@/features/analysis';

export const metadata = {
  title: 'ROI Analysis',
  description: 'Return on investment analysis for real estate properties across different areas.',
};

export default function ROIPage() {
  return <ROIAnalysis />;
}
